# Copy this file to server/.env and fill in your keys.
# Without OPENAI_API_KEY, /api/agent-task returns an explicit "no key set" message
# instead of fake data — by design.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
PORT=5174
